# These functions attempt to optimize the manipulation of matricies.



# This function creates a special matrix tha can cache its inverse.

makeCacheMatrix <- function(x = matrix()) {

return(cacheM)

}

# This function takes the output of makeCacheMatrix and computes the inverse.
# This function also checks if the inverse has already been computed, and if so,
# it retrieves the inverse of the matrix from the cache.

cacheSolve <- function(x, ...) {

     if # matrix has already been inverted...
     then
     newmatrix <- cacheM
     else
     newmatrix  <-  # get the inverse here
     
     return(newmatrix) ## Return a matrix that is the inverse of 'x'
}
